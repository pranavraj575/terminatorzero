import torch
from torch import nn
from networks.ffn import FFN


class PairwisePolicy(nn.Module):
    """
    creates a policy from a (batch size,
    """

    def __init__(self, embedding_dim, hidden_layers, ):
        pass

    def forward(self, X, move_indices):
        pass
